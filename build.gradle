apply plugin: 'idea'

idea {
	project {
		languageLevel = '1.8'
	}
}

subprojects {
	apply plugin: 'java'
	apply plugin: 'idea'

	sourceCompatibility = 1.8

	repositories {
	  mavenCentral()
	}
}

task wrapper(type: Wrapper) {
  gradleVersion = '1.12'
}